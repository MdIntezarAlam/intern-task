# start command
install expo app in phone 
    run this command in terminal => npm install
      run this in terminal start command=> npx expo start
                   scan phone expo app to command scanner


node_modules/
.expo/
dist/
npm-debug.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/

# macOS
.DS_Store

# Temporary files created by Metro to check the health of the file watcher
.metro-health-check*
