# how to run
install expo app in phone 
    run this command in pc terminal=> npm install
      run this in pc terminal start command=> npx expo start
                simply  scan phone expo scanner to command scanner

that's it